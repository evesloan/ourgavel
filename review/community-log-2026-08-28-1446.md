# Community & Moderation log — 2026-08-28 14:46 UTC

## STEP 2 — MODERATION (never skipped): ONE REMOVAL, legal risk

**Removed from the Fernandez case ticker — 2 auto-published items, post-conviction child-sex-crime charges:**
1. Action News Jax — "Mario Fernandez Saldana, man convicted in Jared Bridegan's murder, now accused of child molestation"
2. News4JAX — "Mario Fernandez facing sex crime charges after conviction in Jared Bridegan murder"

**Rule applied:** EDITORIAL §5 + the lead's 14:35 UTC ruling (to-lead). The lead ruled these
post-conviction charges stay **off the Fernandez record** — "a separate uncharged-in-this-case
matter — keep it off"; they belong only on the Gardner board as the defense's attributed filing.
`screen.js` did not hold them (they read as fair-report on a convicted defendant), so the backstop
did. Child-molestation accusations are high-harm-if-wrong and this is a different matter from the
Bridegan murder the page documents.

**Durability:** removed from `items`; the full `seen` array is preserved and both URL-hashes
(`tgeoav`, `u38p6e`) are already in it (27 entries, cap 600), so poll.js line 129 suppresses
re-add for these exact URLs. **Gap flagged to dev:** a syndication/redirect variant with a
different canonical URL would slip the seen-set — the durable fix is the ticker-relevance /
exclusion rebuild already in dev's backlog. Recommend a Fernandez `weakKeywords`/exclusion on
`molestation|sex crime|lewd` so it holds against URL churn.

**Surfaces swept (all 5 cases + every held surface), against the pulse mirror (synced 14:15:20Z):**
- `report`-labelled issues: **0**. `needs-review` queue: **0**.
- `held-comments.json`: `held: []`, `heldSeeds: []`, `cleared: []`.
- All 5 `community.json`: **0/0** nodes/edges. All 5 `threads.json`: **0** threads.
- `health.json`: `brokenCases: []`.
- Only open non-handoff issue is discussion **#13** (Clancy c-7, "Why was she on so many
  different drugs?" / "our medical system is broken") — ruled **§5-clean** again (criticism of an
  institution, no private individual accused; Clancy `community.json` has 0 nodes so it stays
  detached from the board). **KEEP.**

**STEP 2.3 backstop — every auto-published ticker item:** ran all case tickers through `screen.js`;
the 2 Fernandez items above were the only new flags, both handled by removal. Fernandez GUILTY
headlines fire correctly (verdict on file). Nothing else pulled.

**STEP 2.4 comparator calibrated before trusting the zeros:** `screen.implicationHits` fired on
known-held strings ("the babysitter", "the neighbour did it", "her husband?") and stayed clean on
known-pass institutional criticism ("the medical system is broken", "the hospital never escalated",
"the prosecution overcharged"). Alive, not blind. The field it reads (`items[].headline`) checked
first — 22 items present, not an empty read.

## Verdict watch — CLANCY: NO VERDICT

Deliberations day 2 (resumed Friday Aug 28). `verdict.live-check`: Clancy 74 headlines, **none**
read as a verdict — correct. Lil Durk 30 headlines, none — correct. Fernandez fired 4, all
Fernandez, GUILTY on file — correct, no live trial misread. Engine armed and publishes
independently of the applier. **Clancy stays in blackout.**

## STEP 3 — GROWTH

- **Bridegan/Fernandez outreach note: still BLOCKED.** It waits on dev's Fernandez board
  resolution being LIVE. As of this run the central Q `did-he-hire-the-shooter` is still
  `type:"question" status:"open"` on disk — dev's #19 (re-POST of #18) is in the queue but not yet
  applied. Note released the run the resolution lands.
- **Clancy verdict-moment board readiness holds:** board/embed/reader-path suites green; the board
  is ready to carry a resolution the moment the engine publishes one.

## STEP 4 — VERIFY (run once)

- `preflight.js`: PASS (record intact, all JSON parses, no conflict markers).
- `build.js`: 34 pages / 5 cases; sitemap 29 urls; indexnow key served.
- Full gate, enumerated with `ls scripts/*.test.js scripts/*-test.js`: **19/19 green** on the
  edited tree.
- `verdict.live-check.js`: advisory clean (4 fire, all Fernandez on file; no live trial misread).
- Applier browser-suite fix (`test-browser.js` + 4 suites) confirmed in main at `c1fe8e3` — the
  gate now passes on the Actions runner.

## STEP 5 — SHIP

**community handoff — 1 data file + this log:**
- `data/cases/mario-fernandez-bridegan/ticker.json` — 2 items removed (see STEP 2); `seen` preserved.
- `review/community-log-2026-08-28-1446.md` — this log.

Tree green before POST. Diff is the ticker only (14 deletions).

**#16 board-corrections re-POST — HELD this run, not lost.** The lead's "same payload is fine"
can't be recovered from here: GitHub API is blocked for this session (proxy: "access not enabled")
and github.com is robots-disallowed to WebFetch, so the closed #16 issue body is unreadable, and
no #16 payload is stored in the project docs. Reconstructing the full board-corrections feature
from the spec is real work with no pending correction to serve today, so it is not worth crowding
the freshly-recovered queue behind dev's #19 and seo's Gardner re-POST. Flagged to lead: either
re-attach GitHub read access (add_repo) so the exact payload can be re-POSTed, or I rebuild it
against green main next run.
