# Dev run — 2026-09-02 00:34 UTC

**Lane:** development & record. **Base:** origin `d41da4a` (pulse `2026-09-02T00:26:31Z`; re-verified at
POST time — a pulse landed mid-run touching only `data/` ticker/queue/thread files, my target scripts
untouched, merged clean). **Node 22, suite run once, no browser (no UI change), budget-lean.**

## Headline — re-cut the silent-`watch` escalation fix (was #51, failed to apply)

Community (09-01 22:40) and the still-owed ledger confirmed dev #51 **did not apply to origin**:
`watchWarrantsEscalation` absent from `verdict.js`, `verdict-escalation.test.js` absent (suite was 20),
`poll.js`'s `watch` branch still silently `pending:null` with no red-lane issue. #51 is not in the
open-issue sync, so it was closed `handoff-failed`. WebFetch of the issue page is gated in this
unattended run, so the exact gate error wasn't readable — so I re-cut from scratch against current
origin rather than resubmit the same bytes, and hardened the one thing most likely to have failed a
node-20 applier gate: **the new test no longer reads any live `ticker.json`.** The pulse rewrites those
files every 15 min, so a test coupled to live data is racy against the applier (green at build, red at
apply). This suite is 100% synthetic fixtures.

### The defect (unchanged from #51's diagnosis)
A real verdict that only ever reaches 2 of the 3 required independent newsroom families inside the 12h
window makes `assess()` return `watch` every pulse. `poll.js`'s `watch` branch reset state and logged
"below threshold" — **no publish (correct), no alert (the bug).** That is exactly how the flagship
Tupac/Davis GUILTY went unhandled every pulse. `conflict` and `split` already escalate; `watch` did not.

### The fix (additive, never publishes)
- `verdict.js`: new pure predicate `watchWarrantsEscalation(v)` + `WATCH_ESCALATE_MIN = 2`. True only
  when `status==='watch'`, an outcome is set, and ≥2 independent families carry it. `assess()` already
  guarantees a `watch` has zero rivals (any rival → `conflict`), so a positive is one-sided by
  construction. Exported.
- `poll.js`: the `watch` branch now, when `!CASE.verdict && TOKEN && state.alerted!=='watch' &&
  watchWarrantsEscalation(v)`, opens ONE red-lane `verdict-watch` issue naming the outlets and
  headlines, and sets `alerted:'watch'` (fires **once per case**). It **publishes nothing and sets no
  verdict** — §3b keeps the banner the engine's alone. The issue body spells out the RED LANE: a human
  confirms against sources, then aftermath on approval OR the engine self-fires once a 3rd fresh family
  lands in-window. Below-`WATCH_ESCALATE_MIN` watches still log silently as before.
- `verdict-escalation.test.js`: **26 assertions, all synthetic.** Predicate unit tests (2-family →
  escalate; 1-family → no; ready/conflict/split/none/null/malformed → no) and end-to-end through
  `assess()`: the 2-family GUILTY "Tupac shape" escalates; same wire on two mastheads / one copy across
  two families / a 3rd family (→ready) / a rival (→conflict) / a decided split / stale-only / a
  co-defendant under subject-scoping all correctly do **not** escalate.

### Next-pulse expectation
On the next real pulse, Tupac's live `assess()` (GUILTY 2/3 in-window) returns `watch` →
`watchWarrantsEscalation` true → **one** red-lane `verdict-watch` issue opens for Tupac, surfacing the
missed flagship verdict into the queue as Eve's clean approval anchor. No other active case is at 2+
one-sided families, so none spuriously escalates. Verify `handoff-applied` + the new issue next run.

## The record (STEP 2)
- **Tupac:** verdict is real-in-world but RED-LANE (engine at 2/3, banner not fired, no Eve approval,
  this is an unattended run). Aftermath (board resolve + closing day + phase/statusNow) stays STAGED —
  **did not half-update `phase`** ("closing arguments Monday, Aug. 31" is stale but changing it under a
  not-yet-fired banner makes the site inconsistent; prior dev run's standing call, kept). My escalation
  fix is precisely what turns this stuck state into a surfaced, actionable one.
- **Clancy:** jury deadlocked, judge ordered continued deliberations — not a verdict; engine correctly
  quiet; not escalable at 1 family. No new cited day entry addable this run (cloud egress can't fetch
  feeds; WebFetch gated — the pulse folds ticker, not me).
- No safe record change on the other 5 cases this run.

## Verify — full gate GREEN (once, off origin `d41da4a`)
preflight ✅ · build ✅ **84pp/7** (sitemap 77/57, IndexNow key served) · **all 21** `*.test.js`/`-test.js`
✅ (enumerated `ls`; +1 new `verdict-escalation.test.js`; pass=21 fail=0) · audit ✅ · live-check advisory
exit 0 (308 headlines, 10 read as verdict, **2 need a human eye = the real Tupac/Fernandez verdict
headlines the engine can't self-fire — dev/RED-LANE, correct**). Targeted re-verify of the two touched
verdict suites re-run green on the merged post-apply tree.

## Ship
Plan `{v:1, commit:d41da4a, files:[verdict.js, poll.js, verdict-escalation.test.js, this log]}` (scripts/
+ review/ only) → gzip → SHA-256 → base64 → relay POST (browser UA). Verify `handoff-applied` next run;
if failed twice, the exact gate error should be readable then and I go surgical.

## Not touched (recommend, do NOT do unattended)
Widening GUILTY classify to catch `convicts/convicted <name> of <crime>` (NPR "convicts a man of…",
News3LV "why jurors convicted Duane Davis…" still classify null — a 3rd family would let the engine
self-fire Tupac). Highest-risk change in the codebase; must be proven against ALL stored headlines in a
focused run, never bundled. Still owed.
