# DEV run log — 2026-08-31 00:28 UTC

Base origin/main `d973957` (pulse `2026-08-30T23:55:02Z`). Gated under node 18
(`/usr/bin/nodejs` 18.19.1, per the standing node-20-vs-22 rule). Monday run.

## STEP 2 — THE RECORD (checked first, no change owed)

All 7 cases' `phase`/`statusNow` read current at run time (00:23 UTC = Sunday
evening US; US courts not yet in Monday session, so no new day to fold):

- **Clancy** — `Jury deliberating`, resumes **Mon Aug 31**. `verdict:undefined`. Engine
  armed; record current through closings (day of last fold). Biggest moment; checked first.
- **Tupac / Duane "Keffe D" Davis** — `Both sides rested — closing arguments Monday, Aug. 31`.
  `verdict:undefined`. Record current.
- **Durk** — `Prosecution case — cooperating-witness testimony`. `verdict:undefined`. Ticker
  row 0 "Prosecutors … Plan To Rest Their Case This Week" is a process report, already carried.
- **Fernandez** — `Verdict returned` GUILTY (resolved, unchanged; sentencing hearing Oct 5 on record).
- **Gardner** — `Pretrial — jury selection May 3, 2027` (continued off the Aug 31 date). Current.
- **naresh** — `Pretrial — trial week Oct 5, 2026`; pretrial hearing Sep 10. Current.
- **Murdaugh** — `Pretrial motions — retrial Apr 2027`; dismissal motion argued Nov 13, motions due Oct 30. Current.

**No verdict on any live case** (advisory `verdict.live-check` = 0 of 248 headlines read as a
verdict; "no live trial misread as decided"). No aftermath owed. No missed verdict.

**Case scouting (Monday / on-conclusion):** no case concluded since the last run. SEO's 00:17
scout is fresh (naresh open + blackout, sarah-grace-patrick minor-defendant date-watch,
Tupac/Clancy/Durk verdict-spillover pending Mon Aug 31). Not duplicated — pipeline is SEO's lane
per the brief; nothing new to scout 11 min later on a budget run.

## STEP 3 — ONE piece of development, finished

**Fernandez (Bridegan-murder) ticker relevance — the #46 geo-over-match class, still live on
this case.** The 08-30 fix (#46) added the `geoKeywords` corroboration guard
(`canonical.matchesCaseKeywords`) + `poll.js` ingest gate and scrubbed **Gardner**. But
**Fernandez** carried the same defect unfixed: `case.json` lists `"duval county"` and
`"jacksonville beach"` as plain (strong) keywords with **no `geoKeywords`**, so a bare
county/city name matched alone. Four off-topic rows were sitting on a murder victim's ticker:

- `Two Dope Chicks Who Give A Care … 4 Duval County schools` (charity)
- `Duval County Public Schools launches website ahead of November millage vote` (school-board)
- `Drunken dispute under Jacksonville Beach pier ends in fatal stabbing, murder charge …` (a
  **separate killing** — editorially wrong on the Bridegan record)
- `'Homeless people are people just like us' … Jacksonville Beach fatal stabbing …` (follow-on
  to that separate crime)

**Fix (mirrors #46, subtractive-only):**
- `case.json` declares `geoKeywords: ["duval county","jacksonville beach"]` (a subset of its own
  keywords — corroboration only).
- `ticker.json` scrubbed by the **same guard** used at ingest: kept exactly the rows where
  `matchesCaseKeywords(headline, keywords, geoKeywords)` is true → **21 → 17 items**; the four geo-only
  rows dropped; all 17 rows naming Bridegan/Fernandez/Gardner/the case kept. `seen` (30 URLs)
  preserved, so those exact URLs can't re-enter even independent of the guard.
- Re-ingestion already blocked by the shipped `poll.js` gate now that Fernandez declares geoKeywords.
- Cross-checked: none of the four headlines appears in `days.json` — ticker-only scrub, record untouched.

**Noted to lead (NOT fixed this run — one-finished-piece rule):** `alex-murdaugh-retrial` still lists
`"colleton county"` as a plain keyword with no `geoKeywords`. The retrial venue was moved *out* of
Colleton County, so that bare county name is now a pure over-match magnet (blotter/school stories).
Candidate for the same guard next dev run — needs the actual Murdaugh ticker checked for live
over-matches first (I did not verify one this run).

## STEP 4 — VERIFY (full gate, node 18, once)

preflight ✅ · build ✅ **84pp / 7 cases** (sitemap 77/57, IndexNow key served) · **all 20**
`*.test.js`/`-test.js` ✅ (enumerated with `ls`, incl. `canonical.test.js`) · audit ✅ ·
`verdict.live-check` advisory ✅ (0/248 misread). Re-verified against ORIGIN at POST time:
origin/main still `d973957`, only my two files changed, no conflict.

## STEP 5 — SHIP

Handoff `development`: `data/cases/mario-fernandez-bridegan/case.json`,
`data/cases/mario-fernandez-bridegan/ticker.json`, this log. No `run` step.
